export const DEFAULT_VALUE = 'default';

export const FieldType = {
    TEXT: "text",
    TEXT_AREA: "textArea",
    NUMBER: "number",
    SELECT: "select",
};

export const objectConfig = {
    default: {
        id: DEFAULT_VALUE,
        label: 'Brak'
    },
    //Definicja naszych typów
};